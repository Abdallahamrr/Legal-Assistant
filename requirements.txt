# Legal AI Assistant — Requirements
# Install: pip install -r requirements.txt

# Core LLM & LangChain
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
langgraph>=0.1.0

# Vector Store
faiss-cpu>=1.7.4

# Document Loading
pypdf>=4.0.0
pymupdf>=1.24.0          # PyMuPDF (fitz) — better PDF handling

# Embeddings (HuggingFace alternative)
sentence-transformers>=3.0.0
huggingface-hub>=0.23.0

# Optional: Google Gemini
# google-generativeai>=0.7.0
# langchain-google-genai>=1.0.0

# Optional: DeepSeek (via OpenAI-compatible API)
# openai>=1.30.0  (already included via langchain-openai)

# Evaluation
# ragas>=0.1.0            # Production faithfulness eval (optional)

# Utilities
python-dotenv>=1.0.0
tiktoken>=0.7.0
tqdm>=4.66.0

# API Server (optional — for serving the agent as an endpoint)
# fastapi>=0.111.0
# uvicorn>=0.30.0
